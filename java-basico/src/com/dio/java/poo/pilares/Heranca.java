package dio.java.poo.pilares;

/*
 * Herança é um dos pilares da programação orientada a objetos, permitindo que uma classe
 * herde todos os métodos e atributos de outra classe. No exemplo abaixo, a classe `Heranca`
 * estende (`extends`) a classe `Encapsulamento`, o que significa que `Heranca` agora possui
 * todos os métodos e atributos de `Encapsulamento`, como se fossem definidos diretamente dentro
 * dela. Isso facilita o reuso de código e permite que a classe `Heranca` adicione ou sobrescreva
 * funcionalidades conforme necessário.
 */
public class Heranca extends Encapsulamento {
    // A classe Heranca agora possui todos os métodos públicos e protegidos de
    // Encapsulamento

}
